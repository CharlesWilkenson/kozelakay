[defaults]
# This tells Ansible where to find your hosts by default
inventory = inventory.ini

# Optional: Disable host key checking for your local lab
# (prevents those annoying "Are you sure you want to connect" prompts)
host_key_checking = False